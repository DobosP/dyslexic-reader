gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
# ...except the non-secret DEBUG keystore, committed for stable debug signing
# so debug APKs can be installed over each other as updates.
!app/debug.keystore
